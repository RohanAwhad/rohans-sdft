# EXPERIMENTS LOG

---

## #E018 [DONE]

1. **Motivation:** First systematic sweep. Testing aggressive LR + EMA teacher + high temp.
2. **Hypothesis:** 3e-5 LR with EMA self-distillation and temp=1.0 can improve over baseline.
3. **Prediction:** Accuracy should climb over 3 epochs. Refuted if ep1 < 0.60 or declining trajectory.
4. **Design:** LR=3e-5, cosine, IS_CAP=2.0, EMA(0.01) teacher, temp=1.0, enriched, 3ep k400.
5. **Results:** ep1=0.6391, ep2=0.6194, ep3=0.6822. Δ(1→3)=+4.3pp.
6. **Analysis:** Ep2 dipped below ep1 — unstable. Recovery at ep3 but still far from baseline.
7. **Conclusion:** INCONCLUSIVE. REVERT. EMA + high LR + high temp is too noisy.
8. **New questions:** Is the EMA teacher or the high temp the problem?

---

## #E019 [DONE]

1. **Motivation:** Replace EMA with frozen 120b teacher, keep cosine schedule.
2. **Hypothesis:** 120b frozen teacher provides better signal than EMA at 1e-5 with cosine warmup.
3. **Prediction:** Smoother trajectory than E018, ep3 > 0.70.
4. **Design:** LR=1e-5, cosine, IS_CAP=2.0, 120b teacher, temp=0.7, enriched, 3ep k400.
5. **Results:** ep1=0.6391, ep2=0.6930, ep3=0.7056. Δ(1→3)=+6.7pp.
6. **Analysis:** Strong upward trend. 120b teacher clearly better than EMA. Cosine warmup delayed ep1.
7. **Conclusion:** WEAKLY SUPPORTED. KEEP. 120b teacher validated, but cosine delays convergence.
8. **New questions:** Would constant schedule start higher at ep1?

---

## #E020 [DONE]

1. **Motivation:** Test constant schedule vs cosine at 1e-5.
2. **Hypothesis:** Constant LR avoids the slow cosine warmup, giving higher ep1.
3. **Prediction:** ep1 > 0.68 (higher than E019's 0.6391), flat or improving trend.
4. **Design:** LR=1e-5, constant, IS_CAP=2.0, 120b teacher, temp=0.7, enriched, 3ep k400.
5. **Results:** ep1=0.7056, ep2=0.7127, ep3=0.7038. Δ(1→3)=-0.2pp.
6. **Analysis:** ep1 much higher than E019 (confirming constant > cosine for early epochs). But flat — no improvement over 3 epochs.
7. **Conclusion:** SUPPORTED (constant beats cosine at ep1). KEEP. But 1e-5 doesn't scale with more training.
8. **New questions:** Does higher LR (2e-5) scale better over epochs?

---

## #E021 [DONE]

1. **Motivation:** Isolate teacher effect — EMA vs frozen 120b at 1e-5.
2. **Hypothesis:** EMA self-distillation is comparable to 120b teacher at low LR.
3. **Prediction:** ep3 within 2pp of E020 (0.7038).
4. **Design:** LR=1e-5, constant, IS_CAP=2.0, EMA teacher, temp=0.7, enriched, 3ep k400.
5. **Results:** ep1=0.6912, ep2=0.6930, ep3=0.6912. Δ(1→3)=0pp.
6. **Analysis:** Completely flat. EMA teacher at 1e-5 doesn't improve at all.
7. **Conclusion:** REFUTED. REVERT. 120b teacher essential even at low LR.
8. **New questions:** None — EMA eliminated as viable teacher option.

---

## #E022 [DONE]

1. **Motivation:** Test IS weighting off vs on at 1e-5.
2. **Hypothesis:** IS weighting matters for gradient quality.
3. **Prediction:** Without IS, accuracy changes by >2pp vs E020.
4. **Design:** LR=1e-5, constant, IS off, 120b teacher, temp=0.7, enriched, 3ep k400.
5. **Results:** ep1=0.6912, ep2=0.6822, ep3=0.6894. Δ(1→3)=-0.2pp.
6. **Analysis:** IS on/off makes no difference at 1e-5. Both flat.
7. **Conclusion:** INCONCLUSIVE. The LR is too low for IS to matter.
8. **New questions:** Does IS matter at higher LR where gradients are larger?

---

## #E023 [DONE]

1. **Motivation:** Test 2e-5 LR — higher than 1e-5 which plateaus, lower than 3e-5 which is noisy.
2. **Hypothesis:** 2e-5 is the sweet spot for learning with thinking mode.
3. **Prediction:** ep3 > 0.70 with upward trajectory across all 3 epochs.
4. **Design:** LR=2e-5, constant, IS_CAP=2.0, 120b teacher, temp=0.7, enriched, 3ep k400.
5. **Results:** ep1=0.6230, ep2=0.6966, ep3=0.7110. Δ(1→3)=+8.8pp.
6. **Analysis:** Strongest trajectory of any run. +8.8pp over 3 epochs. ep1 dip is expected — higher LR destabilizes initially but recovers strongly.
7. **Conclusion:** SUPPORTED. KEEP. 2e-5 is the scaling LR. Best 3-epoch result.
8. **New questions:** Does it continue scaling past epoch 3? Does it beat baseline at epoch 10?

---

## #E024 [DONE]

1. **Motivation:** Test EMA teacher at 2e-5 (the scaling LR).
2. **Hypothesis:** EMA self-distillation can work at 2e-5 where the LR is high enough.
3. **Prediction:** ep3 within 3pp of E023 (0.7110).
4. **Design:** LR=2e-5, constant, IS_CAP=2.0, EMA teacher, temp=0.7, enriched, 3ep k400.
5. **Results:** ep1=0.6786, ep2=0.6715, ep3=0.6643. Δ(1→3)=-1.4pp.
6. **Analysis:** Declining trajectory. EMA teacher fails at 2e-5 — the student drifts from itself.
7. **Conclusion:** REFUTED. REVERT. 120b frozen teacher is essential at all LRs.
8. **New questions:** None — EMA teacher conclusively eliminated.

---

## #E025 [DONE]

1. **Motivation:** Test temp=1.0 at 2e-5 (E023 used 0.7).
2. **Hypothesis:** Higher rollout temperature increases exploration, improving accuracy.
3. **Prediction:** ep3 > 0.70, comparable to E023.
4. **Design:** LR=2e-5, constant, IS_CAP=2.0, 120b teacher, temp=1.0, enriched, 3ep k400.
5. **Results:** ep1=0.6876, ep2=0.6930, ep3=0.6589. Δ(1→3)=-2.9pp.
6. **Analysis:** Declining trajectory. Noisy rollouts at temp=1.0 hurt — the student gets worse signal.
7. **Conclusion:** REFUTED. REVERT. temp=0.7 >> temp=1.0 at 2e-5.
8. **New questions:** Is there a sweet spot between 0.7 and 1.0? Or is lower always better?

---

## #E026 [DONE]

1. **Motivation:** Test IS_CAP=5.0 at 2e-5 (E023 used 2.0).
2. **Hypothesis:** Higher IS cap allows more gradient from high-ratio samples.
3. **Prediction:** ep3 > 0.70, trajectory at least as good as E023.
4. **Design:** LR=2e-5, constant, IS_CAP=5.0, 120b teacher, temp=0.7, enriched, 3ep k400.
5. **Results:** ep1=0.6230, ep2=0.6481, ep3=0.6553. Δ(1→3)=+3.2pp.
6. **Analysis:** Much weaker than E023 (+3.2pp vs +8.8pp). IS_CAP=5.0 is worse than 2.0 at 3 epochs.
7. **Conclusion:** REFUTED at 3 epochs. REVERT. IS_CAP=2.0 is better for short runs.
8. **New questions:** Could 5.0 perform differently over longer training (more steps)?

---

## #E027 [DONE]

1. **Motivation:** Test IS_CAP=1.0 at 2e-5.
2. **Hypothesis:** Tighter IS clipping stabilizes training.
3. **Prediction:** Less zigzag, ep3 > 0.70.
4. **Design:** LR=2e-5, constant, IS_CAP=1.0, 120b teacher, temp=0.7, enriched, 3ep k400.
5. **Results:** ep1=0.6643, ep2=0.6858, ep3=0.6804. Δ(1→3)=+1.6pp.
6. **Analysis:** Clips ~20% of samples. Too aggressive — suppresses useful gradient signal.
7. **Conclusion:** REFUTED. REVERT. IS_CAP=1.0 clips too much.
8. **New questions:** IS_CAP=2.0 sweet spot confirmed at 3 epochs.

---

## #E028 [DONE]

1. **Motivation:** Test GRAD_ACCUM=16 (half of E023's 32).
2. **Hypothesis:** Smaller effective batch size gives more frequent updates.
3. **Prediction:** Faster convergence, ep3 > 0.70.
4. **Design:** LR=2e-5, constant, IS_CAP=2.0, 120b teacher, temp=0.7, enriched, GRAD_ACCUM=16, 3ep k400.
5. **Results:** ep1=0.6697, ep2=0.6804, ep3=0.6948. Δ(1→3)=+2.5pp.
6. **Analysis:** Weaker than E023 (+2.5pp vs +8.8pp). Noisier gradients from smaller batch.
7. **Conclusion:** REFUTED. REVERT. GRAD_ACCUM=32 is better.
8. **New questions:** None.

---

## #E029 [DONE]

1. **Motivation:** Test LR=1.5e-5 — between the flat 1e-5 and scaling 2e-5.
2. **Hypothesis:** 1.5e-5 may be a smoother scaling point.
3. **Prediction:** Upward trajectory, ep3 > 0.70.
4. **Design:** LR=1.5e-5, constant, IS_CAP=2.0, 120b teacher, temp=0.7, enriched, 3ep k400.
5. **Results:** ep1=0.6894, ep2=0.7092, ep3=0.6948. Δ(1→3)=+0.5pp.
6. **Analysis:** Nearly flat. 1.5e-5 behaves like 1e-5 — doesn't scale.
7. **Conclusion:** REFUTED. REVERT. Only 2e-5 scales with thinking mode.
8. **New questions:** None — 2e-5 confirmed as the only viable LR.

---

## #E030 [DONE]

1. **Motivation:** 10-epoch validation of E023's config (best 3-epoch result).
2. **Hypothesis:** The +8.8pp trajectory continues beyond epoch 3, eventually beating baseline 0.7415.
3. **Prediction:** Accuracy reaches >0.74 by epoch 10.
4. **Design:** LR=2e-5, constant, IS_CAP=2.0, 120b teacher, temp=0.7, enriched, 10ep k400.
5. **Results:** ep1=0.6697, ep2=0.6822, ep3=0.6894, ep4=0.6966, ep5=0.6858, ep6=0.6930, ep7=0.6912, ep8=0.6984. Ep9-10 lost to disk full.
6. **Analysis:** Plateaued at 0.69-0.70 with zigzag pattern. Never approached baseline. The 3-epoch trajectory did not sustain.
7. **Conclusion:** REFUTED. The k400 subset is too small — model overfits and oscillates.
8. **New questions:** Can online_feedback provide richer per-step signal to break the plateau?

---

## #E031 [DONE]

1. **Motivation:** Test online_feedback mode — reflector LLM provides per-step feedback.
2. **Hypothesis:** online_feedback gives richer conditioning signal, pushing accuracy higher than enriched.
3. **Prediction:** ep3 > 0.7110 (E023's ep3 with enriched).
4. **Design:** LR=2e-5, constant, IS_CAP=2.0, 120b teacher, temp=0.7, online_feedback, 3ep k400.
5. **Results:** ep1=0.6338, ep2=0.6750, ep3=0.6517. Δ(1→3)=+1.8pp.
6. **Analysis:** Much worse than enriched E023 (+1.8pp vs +8.8pp). Ep3 dipped from ep2.
7. **Conclusion:** REFUTED at 3 epochs with constant schedule. online_feedback underperforms enriched.
8. **New questions:** Does cosine schedule help online_feedback?

---

## #E032 [DONE]

1. **Motivation:** Test cosine schedule with online_feedback.
2. **Hypothesis:** Cosine warmup stabilizes online_feedback's richer but noisier signal.
3. **Prediction:** ep3 > E031's 0.6517.
4. **Design:** LR=2e-5, cosine, IS_CAP=2.0, 120b teacher, temp=0.7, online_feedback, 3ep k400.
5. **Results:** ep1=0.6607, ep2=0.6535, ep3=0.6948. Δ(1→3)=+3.4pp.
6. **Analysis:** Better than constant+online_fb (E031), but still below enriched (E023).
7. **Conclusion:** WEAKLY SUPPORTED. Cosine helps online_feedback but doesn't close the gap with enriched.
8. **New questions:** Does online_feedback perform better over longer training (10 epochs)?

---

## #E033 [DONE]

1. **Motivation:** 10-epoch validation of online_feedback.
2. **Hypothesis:** online_feedback's richer signal accumulates benefit over more epochs, eventually beating enriched.
3. **Prediction:** Peak accuracy > 0.7289 (E034's enriched 10ep peak, run concurrently). Ideally >0.7415 baseline.
4. **Design:** LR=2e-5, constant, IS_CAP=2.0, 120b teacher, temp=0.7, online_feedback, 10ep k400.
5. **Results:** ep1=0.6984, ep2=0.7092, ep3=0.6804, ep4=0.6984, ep5=0.6750, ep6=0.6786, ep7=0.7056, ep8=0.7092, ep9=0.7253, ep10=0.7020.
6. **Analysis:** Peak 0.7253@ep9. Same zigzag plateau as E030 (enriched). online_feedback didn't break through.
7. **Conclusion:** REFUTED. online_feedback doesn't outperform enriched over 10 epochs on k400. REVERT.
8. **New questions:** Is the k400 plateau a dataset size issue, not a signal quality issue?

---

## #E034 [DONE]

1. **Motivation:** Test cosine schedule over 10 epochs with enriched.
2. **Hypothesis:** Cosine decay prevents late-epoch oscillation.
3. **Prediction:** Less zigzag than E030 (constant), peak > 0.70.
4. **Design:** LR=2e-5, cosine, IS_CAP=2.0, 120b teacher, temp=0.7, enriched, 10ep k400.
5. **Results:** ep1=0.6804, ep2=0.6589, ep3=0.6804, ep4=0.7056, ep5=0.7181, ep6=0.6966, ep7=0.7289, ep8=0.7163, ep9=0.7235, ep10=0.7163.
6. **Analysis:** Peak 0.7289@ep7 — highest of any IS run. Less zigzag in late epochs than E030. Cosine helps.
7. **Conclusion:** WEAKLY SUPPORTED. KEEP. Cosine is better than constant for 10-epoch runs. But still doesn't beat baseline.
8. **New questions:** Is IS clipping the bottleneck? Non-IS runs (3, 4, 13) beat baseline.

---

## #E035 [DONE]

1. **Motivation:** Test IS_CAP=5.0 + temp=1.2 (loosen IS clipping + more rollout diversity).
2. **Hypothesis:** Higher IS cap + higher temp unblocks the 0.73 plateau by allowing stronger gradient signal and more exploration.
3. **Prediction:** Peak > 0.74 by epoch 10. Smoother trajectory than E030/E034.
4. **Design:** LR=2e-5, constant, IS_CAP=5.0, 120b teacher, temp=1.2, enriched, 10ep k400.
5. **Results:** ep1=0.6194, ep2=0.6230, ep3=0.6589, ep4=0.6517, ep5=0.6822, ep6=0.7235, ep7=0.7235, ep8=0.6894, ep9=0.6876, ep10=0.6858.
6. **Analysis:** Smooth climb to 0.7235@ep6-7, then collapsed to 0.69 by ep10. Constant schedule can't sustain gains at higher IS_CAP.
7. **Conclusion:** INCONCLUSIVE. The climb was promising but the collapse kills it. Need cosine to prevent late-epoch decay.
8. **New questions:** Does cosine + IS_CAP=5.0 prevent the collapse?

---

## #E036 [DONE]

1. **Motivation:** Test locked config (cosine + online_fb + IS_CAP=5.0 + temp=1.2).
2. **Hypothesis:** Cosine prevents the late-epoch collapse seen in E035 while IS_CAP=5.0 provides stronger gradient.
3. **Prediction:** Peak > 0.74. No collapse in ep8-10.
4. **Design:** LR=2e-5, cosine, IS_CAP=5.0, 120b teacher, temp=1.2, online_feedback, 10ep k400.
5. **Results:** ep1=0.6338, ep2=0.6768, ep3=0.7092, ep4=0.7235, ep5=0.6768, ep6=0.7110, ep7=0.7163, ep8=0.7074, ep9=0.7127, ep10=0.7110.
6. **Analysis:** Peak 0.7235@ep4. Cosine prevented collapse (ep8-10 stable at 0.71). But still didn't break 0.74.
7. **Conclusion:** WEAKLY SUPPORTED (no collapse) but REFUTED (didn't reach 0.74). Same 0.71 plateau.
8. **New questions:** Is the k400 dataset too small? Move to large dataset.

---

## #E037 [KILLED]

1. **Motivation:** First large dataset run. Test if 12.5x more training examples breaks the k400 plateau.
2. **Hypothesis:** More unique examples per epoch provide the gradient diversity needed to push past 0.73.
3. **Prediction:** Accuracy exceeds 0.74 within 3 epochs (~468 steps). Step-50 checkpoints should show steady improvement.
4. **Design:** LR=2e-5, cosine, IS_CAP=2.0, 120b teacher, temp=0.7, enriched, 3ep large dataset (5002 ex), SAVE_EVERY=50.
   **NOTE:** This run used enriched + IS_CAP=2.0 instead of the locked config (online_fb + IS_CAP=5.0). Config error — should have used locked knobs.
5. **Results:** Killed at step 446/468 (mid-epoch-3). step_50=0.6966, step_100=0.6930, step_150=0.7074, step_200=0.7217, step_250=0.7038, step_300=0.7181, step_350=0.7092, step_400=0.7056, epoch_1=0.6697, epoch_2=**0.7307**. Peak 0.7307@epoch_2 (step ~312).
6. **Analysis:** Large dataset shows the same 0.69-0.73 plateau as k400. Peak 0.7307 is the highest of any run with IS, but still below baseline 0.7415. Step-200 peaked at 0.7217, then oscillated. More data did NOT break the plateau. Config was wrong (enriched + IS_CAP=2.0, not locked knobs).
7. **Conclusion:** REFUTED. Data scale alone doesn't break the plateau. The problem is in the loss function / IS mechanism, not dataset size. REVERT to loss-function focus.
8. **New questions:** Is pure reverse-KL the bottleneck? Would an SFT anchor term provide the direct gradient toward correct tokens that reverse-KL lacks?

---

## #E038 [KILLED]

1. **Motivation:** All 20+ IS-enabled reverse-KL runs plateau at 0.69-0.73. Devlogs Run 9 identifies reverse-KL as mode-seeking — "student gets sharper but not smarter." The loss optimizes distribution matching (KL), not correctness (accuracy). An SFT anchor term adds direct supervised gradient on golden answer tokens, bypassing the IS weighting path entirely.
2. **Hypothesis (M5):** Pure reverse-KL optimizes a proxy (KL divergence) that diverges from the target metric (accuracy). Adding λ·NLL(golden_answer) provides a direct gradient toward correct tokens, breaking the plateau.
3. **Prediction:** Peak accuracy > 0.74 by epoch 10. Epoch_1 > 0.72 (faster early ascent than pure reverse-KL, which typically reaches ~0.63-0.67 at ep1). Falsified if: peak ≤ 0.73 while KL loss keeps decreasing (same plateau as E034-E037). Also refuted if epoch_1 > 0.75 then collapses to < 0.70 by epoch 5 (λ too high, SFT dominates and causes overfitting).
4. **Design:** LR=2e-5, cosine, IS_CAP=5.0, 120b teacher, temp=1.2, online_feedback, SFT_LAMBDA=0.1, 10ep k400. Loss = reverse_kl + 0.1 * NLL(golden_answer). Second student forward pass on [prompt + golden_ids] for NLL, backwarded separately under fresh no_sync context (KL backward first to free graph, then NLL forward+backward).
5. **Results:** Killed at step 14 (epoch 2). epoch_1=0.5745. SFT NLL decreasing (5.46→2.52), but KL loss climbing (1.40→1.79). signal_mean dropped to -3.26 at step 8 (student diverging from teacher). Loss spiking at steps 13-14 (1.79).
6. **Analysis:** λ=0.1 is too high — the SFT anchor dominates and destabilizes the KL objective. The two objectives compete: SFT pulls toward memorizing golden answers, KL pulls toward teacher distribution. The SFT signal IS working (NLL decreasing), but at this magnitude it destroys the KL distillation signal. Epoch_1=0.5745 is far below prediction (>0.72) and below any prior run's ep1.
7. **Conclusion:** REFUTED at λ=0.1. The SFT signal is useful but destabilizing at this magnitude. λ needs to be much lower (0.01-0.05). Pre-registered falsification criterion met: "epoch_1 spikes then collapses → λ too high."
8. **New questions:** What λ value balances SFT anchor benefit with KL stability? Try λ=0.01 (10x lower).


---

## #E039 [DONE]

1. **Motivation:** E038 showed SFT anchor at lambda=0.1 destabilizes KL (loss climbing, signal_mean dropped to -3.26, ep1=0.5745). But SFT NLL was decreasing (5.46 to 2.52), confirming the SFT signal works -- it's just too strong. Need 10x lower lambda.
2. **Hypothesis (M5, revised):** At lambda=0.01, the SFT anchor provides a gentle direct gradient toward correct tokens without destabilizing the KL objective. The NLL term should be ~2-4% of total loss (vs ~20-30% at lambda=0.1).
3. **Prediction:** Peak accuracy > 0.74 by epoch 10. Epoch_1 > 0.68 (comparable to pure reverse-KL, not lower). KL loss should decrease normally (not climb). Falsified if: ep1 < 0.60 (SFT still destabilizing) or peak <= 0.73 with KL decreasing normally (SFT anchor doesn't help even at low lambda).
4. **Design:** Same as E038 but SFT_LAMBDA=0.01. LR=2e-5, cosine, IS_CAP=5.0, 120b teacher, temp=1.2, online_feedback, 10ep k400.
5. **Results:** ep1=0.6571, ep2=0.6409, ep3=0.6427, ep4=0.6732, ep5=0.6679, ep6=0.6750, ep7=0.6750, ep8=0.6804, ep9=0.6715, ep10=0.6804. Peak 0.6804@ep8/10.
6. **Analysis:** KL stable (loss ~1.0-1.2, not climbing like E038). SFT NLL decreasing (4.37 to 3.04). But accuracy consistently 2-5pp below E036 (no SFT) at every epoch. E039 peak 0.6804 vs E036 peak 0.7235 (-4.3pp). The SFT anchor at lambda=0.01 doesn't destabilize KL, but it still interferes with the distillation signal, degrading accuracy.
7. **Conclusion:** REFUTED. SFT anchor hypothesis (M5) refuted at both lambda=0.1 (destabilizes KL) and lambda=0.01 (degrades accuracy by 4.3pp). The SFT NLL gradient competes with reverse-KL and degrades distillation. REVERT (SFT_LAMBDA=0.0).
8. **New questions:** Is the per-sequence IS scalar the bottleneck (M4)? Test per-token IS next.

---


---

## #E040 [DONE]

1. **Motivation:** SFT anchor (M5) refuted at both lambda values. Now testing M4 -- the per-sequence IS scalar (ratio_v.mean()) collapses 100x per-token ratio spread into one number, losing granular correction.
2. **Hypothesis (M4, revised):** Per-token IS preserves per-token correction that the per-sequence scalar destroys. The 0.97 ratio mean is an average of tokens ranging from ~0.03 to ~3.8; applying this per-token instead of as a scalar should distribute the IS correction more effectively.
3. **Prediction:** Peak accuracy > 0.74 by epoch 10. is/ratio_std diagnostic shows high variance (>0.3), confirming per-sequence mean was discarding useful signal. Less oscillation in trajectory. Falsified if: peak <= 0.73 with ratio_std < 0.1 (ratios are near-uniform, per-token IS doesn't change anything), or training destabilizes (loss spikes, accuracy drops below 0.65).
4. **Design:** Same locked config as E036 (LR=2e-5, cosine, IS_CAP=5.0, 120b teacher, temp=1.2, online_feedback, 10ep k400) but with IS_PER_TOKEN=1. No SFT anchor (SFT_LAMBDA=0.0). Single code change in chunked_head.py: per-token ratio weighting instead of scalar.
5. **Results:** ep1=0.6104, ep2=0.6804, ep3=0.7074, ep4=0.6894, ep5=0.7217, ep6=0.6984, ep7=0.6786, ep8=0.6858, ep9=0.6930, ep10=0.6930. Peak 0.7217@ep5. ratio_std=0.44-1.54 (CONFIRMED >0.3). ratio_min=0.006, ratio_max=5.0. clip_rate=12-15% (vs 0.3% per-sequence). ratio_mean=1.1-1.5 (vs ~0.97 per-sequence).
6. **Analysis:** Per-token IS accelerated early learning (steepest 3-epoch climb: +9.7pp, reached 0.7217 by ep5 vs E036's 0.6768 at ep5). But training collapsed after ep5 (0.7217 to 0.6930) -- the noisier per-token gradients (12-15% clip rate, high ratio_std) destabilized late-epoch training. Peak 0.7217 is comparable to E036's 0.7235 (within noise). The mechanism (M4) is CONFIRMED -- per-token ratios vary wildly and per-sequence averaging was losing real signal -- but the raw per-token approach is too noisy for stable training.
7. **Conclusion:** INCONCLUSIVE. M4 confirmed (per-token signal was being lost) but raw per-token IS doesn't improve peak accuracy and causes late collapse. The signal is real but needs stabilization -- possible approaches: lower IS_CAP to reduce clip rate, self-normalized IS weights, or per-token IS with gradient clipping.
8. **New questions:** Would a lower IS_CAP (e.g. 2.0) with per-token IS reduce the clip rate and stabilize late training? Would self-normalized IS (normalize ratios across batch) smooth the per-token signal?

---

## #E041 [DONE]

1. **Motivation:** E040 (per-token IS, cap=5.0) showed per-token signal is real (ratio_std 0.44-1.54) but caused late collapse (0.7217 to 0.6930) from 12-15% clip rate. Test if lower cap (2.0) reduces noise and prevents collapse.
2. **Hypothesis (M6):** Per-token IS with IS_CAP=2.0 reduces clip rate to ~2-4%, stabilizing late training while preserving per-token correction.
3. **Prediction:** Peak > 0.74. No late collapse (ep8-10 within 2pp of peak). Falsified if: peak <= 0.73 or late collapse (>3pp drop from peak to ep10).
4. **Design:** Same as E040 but IS_CAP=2.0. Per-token IS, cosine, online_feedback, 120b teacher, temp=1.2, 10ep k400.
5. **Results:** ep1=0.6032, ep2=0.6661, ep3=0.6679, ep4=0.6948, ep5=0.7038, ep6=0.6858, ep7=0.6822, ep8=0.6930, ep9=0.6930, ep10=0.6715. Peak 0.7038@ep5. Collapsed to 0.6715 by ep10 (-3.2pp). clip_rate=5-18% (still high despite lower cap). ratio_std=0.33-0.57, ratio_mean=1.02-1.05.
6. **Analysis:** Lower cap reduced ratio_std (0.33-0.57 vs E040's 0.44-1.54) but clip_rate remained high (5-18%). Peak was lower (0.7038 vs 0.7217) -- tighter cap trimmed useful signal. Still collapsed in late epochs. Both per-token IS variants (cap=5.0 and cap=2.0) peak at ep5 then decline. The per-sequence IS (E036) stays stable through ep10.
7. **Conclusion:** REFUTED. Lower cap doesn't prevent collapse and reduces peak. Per-token IS is fundamentally less stable than per-sequence IS in late training. The per-token signal is real (M4 confirmed) but the raw per-token approach introduces gradient noise that accumulates over epochs.
8. **New questions:** Is the per-sequence IS scalar actually the right approach after all? E036 (per-seq, cap=5.0) is the most stable run at 0.71-0.72. Should we accept the 0.72-0.73 plateau and focus on other axes (temp, dataset, schedule)? Or try self-normalized IS as a middle ground?


---

## #E042 [RUNNING]

1. **Motivation:** Per-token IS (E040/E041) confirmed per-token signal is real (ratio_std 0.44-1.54) but causes late-epoch collapse. Self-normalized IS normalizes gradient magnitude while preserving per-token correction.
2. **Hypothesis (M6):** Self-normalized per-token IS (kl*ratio).sum()/ratio.sum() preserves per-token correction while preventing the magnitude fluctuations that cause late collapse.
3. **Prediction:** Peak > 0.74. No late collapse (ep8-10 within 2pp of peak). Falsified if: peak <= 0.73 or late collapse (>3pp drop from peak to ep10).
4. **Design:** IS_PER_TOKEN=1, IS_SELF_NORMALIZED=1, IS_CAP=5.0. Same locked config (cosine, online_feedback, 120b teacher, temp=1.2, 10ep k400).
5. **Results:** (pending)
6. **Analysis:** (pending)
7. **Conclusion:** (pending)
8. **New questions:** (pending)

---

## #E042 [DONE]

1. **Motivation:** Per-token IS (E040/E041) confirmed per-token signal is real (ratio_std 0.44-1.54) but causes late-epoch collapse. Self-normalized IS normalizes gradient magnitude while preserving per-token correction.
2. **Hypothesis (M6):** Self-normalized per-token IS (kl*ratio).sum()/ratio.sum() preserves per-token correction while preventing the magnitude fluctuations that cause late collapse.
3. **Prediction:** Peak > 0.74. No late collapse (ep8-10 within 2pp of peak). Falsified if: peak <= 0.73 or late collapse (>3pp drop from peak to ep10).
4. **Design:** IS_PER_TOKEN=1, IS_SELF_NORMALIZED=1, IS_CAP=5.0. Same locked config (cosine, online_feedback, 120b teacher, temp=1.2, 10ep k400).
5. **Results:** ep1=0.5889, ep2=0.6230, ep3=0.6535, ep4=0.6984, ep5=0.7056, ep6=0.7038, ep7=0.7038, ep8=0.7056, ep9=0.6948, ep10=0.7038. Peak 0.7056@ep5. Drop from peak to ep10: -1.8pp (stable!). ratio_std=0.19-1.49, ratio_mean=1.0-1.6.
6. **Analysis:** Self-normalization WORKS for stability -- only 1.8pp drop from peak to ep10 (vs E040's 2.9pp, E041's 3.2pp). But peak is lower (0.7056 vs E040's 0.7217, E036's 0.7235). The normalization smooths gradients but also reduces the effective signal strength. E042 is the most stable per-token IS variant but doesn't break the plateau.
7. **Conclusion:** WEAKLY SUPPORTED (stability confirmed) but REFUTED (peak <= 0.73). Self-normalized IS prevents collapse but at the cost of peak accuracy. The per-token IS approach (raw, lower cap, self-normalized) has been thoroughly tested -- none break the 0.73 plateau. REVERT to per-sequence IS.
8. **New questions:** All IS loss-function variants (per-sequence, per-token raw, per-token lower cap, per-token self-normalized) plateau at 0.70-0.73. Is the problem NOT in the IS aggregation but in the IS mechanism itself? Should we test IS on/off at the locked config? (Requires GOAL.md amendment since IS_WEIGHTING=1 is locked.) Or explore other loss functions (JSD, length normalization)?


---

## #E043 [PLANNED]

1. **Motivation:** All IS variants exhausted (E040-E042). SFT anchor refuted (E038-E039). The plateau is invariant to gradient weighting. JSD changes WHAT the loss optimizes (divergence geometry) rather than HOW gradients are weighted. Reverse-KL is mode-seeking (devlogs Run 9); JSD is mass-covering and bounded.
2. **Hypothesis (M7):** The reverse-KL mode-seeking property causes the 0.73 plateau. JSD (symmetric, mass-covering, bounded) reduces mode-seeking and breaks through.
3. **Prediction:** Peak > 0.74 by epoch 10. signal_mean trends toward 0 (from -0.8/-1.0 toward -0.3). Less late-epoch oscillation. Falsified if: peak <= 0.73 (divergence geometry is not the bottleneck) OR signal_mean stays below -0.6 (mode-seeking persists).
4. **Design:** DIVERGENCE=jsd, per-sequence IS (revert IS_PER_TOKEN=0, IS_SELF_NORMALIZED=0), IS_CAP=5.0, LR=2e-5, cosine, online_feedback, 120b teacher, temp=1.2, 10ep k400. Parity test required before run.
5. **Results:** (pending)
6. **Analysis:** (pending)
7. **Conclusion:** (pending)
8. **New questions:** (pending)


---

## #E043 [KILLED]

1. **Motivation:** All IS variants exhausted. JSD changes the divergence geometry (mass-covering vs mode-seeking) rather than gradient weighting. Reverse-KL is mode-seeking (devlogs Run 9); JSD is symmetric, bounded, mass-covering.
2. **Hypothesis (M7):** The reverse-KL mode-seeking property causes the 0.73 plateau. JSD reduces mode-seeking and breaks through.
3. **Prediction:** Peak > 0.74 by epoch 10. signal_mean trends toward 0 (from -0.8/-1.0 toward -0.3). Falsified if: peak <= 0.73 OR signal_mean stays below -0.6.
4. **Design:** DIVERGENCE=jsd, per-sequence IS, IS_CAP=5.0, LR=2e-5, cosine, online_feedback, 120b teacher, temp=1.2, 10ep k400. Parity-tested backward (2.7e-7 relative error).
5. **Results (partial, killed at ep6):** ep1=0.5458, ep2=0.6535, ep3=0.6786, ep4=0.7110, ep5=0.7092. signal_mean: ~-0.9 at steps 1-9, trending to ~-0.8 by steps 35-59 (slight improvement but not reaching -0.3 target). Loss stable at ~0.15 (bounded JSD).
6. **Analysis:** JSD trajectory comparable to reverse-KL (E036: 0.6338->0.6768->0.7092->0.7235->0.6768). At ep5, JSD (0.7092) is between E036 ep4 (0.7235) and ep5 (0.6768). signal_mean slightly better than reverse-KL (~-0.8 vs ~-0.9) but not dramatically different. Killed before epoch 10 to shift focus to async rollout long runs.
7. **Conclusion:** INCONCLUSIVE (killed early). JSD shows comparable trajectory to reverse-KL at 5 epochs. signal_mean slightly improved but not enough to confirm the mass-covering hypothesis. Would need 10+ epochs to fully evaluate.
8. **New questions:** Would JSD break through with more epochs? Shift focus to async rollout long runs on promising configs (E036) to test if longer training breaks the plateau.


---

## #E045 [DONE]

1. **Motivation:** All loss-function axes exhausted (IS aggregation, SFT anchor, JSD). 25+ experiments plateau at 0.69-0.73. New axis: inject retrieved context into the student's training prompt. A retriever at localhost:9090 provides domain-relevant chunks per question. If the student's bottleneck is missing knowledge (not loss geometry), context injection provides the missing signal that no loss modification can.
2. **Hypothesis (M8):** The 0.73 plateau is caused by insufficient domain knowledge in the training prompt, not loss geometry. Injecting retrieved context into the last human message (format: `Context:\n{chunks}\n\nQuestion:\n{original_question}`) provides factual grounding that breaks the plateau.
3. **Prediction:** Peak > 0.74 by epoch 5. Sustained above 0.74 through epoch 10. Falsified if: peak <= 0.73 (context injection doesn't help) OR peak > 0.74 but collapses below 0.73 by epoch 10 (context helps early but doesn't sustain).
4. **Design:** Locked config (LR=2e-5, cosine, IS_CAP=5.0, 120b teacher, temp=1.2, online_feedback) + ASYNC_ROLLOUT=1 + context-injected k400 dataset (`subset_k400_subset_with_context.jsonl`, 400 examples preprocessed with retriever k=5). 10 epochs, 12 steps/epoch = 120 total steps.
5. **Results (full, run_45_10ep, recovered from eval_results after campaign was paused):** ep1=0.7235, ep2=0.7056, ep3=0.7289, ep4=0.7235, ep5=**0.7433**, ep6=0.7235, ep7=0.7253, ep8=0.7217, ep9=0.7289, ep10=0.7289.
6. **Analysis:** FIRST RUN TO CROSS BASELINE 0.7415. Peak epoch 5 = 0.7433 (+0.18pp). Epochs 6-10 settled into a stable 0.7217-0.7289 band — no late-epoch collapse (unlike E035's IS_CAP=5.0 constant-schedule collapse), but no further improvement past the epoch-5 peak either. Mean of ep6-10 = 0.7257, still within the noise floor of baseline. The trajectory is radically different from all prior runs: ep1 starts at 0.7235 (vs typical 0.60-0.68 for locked config without context), never drops below 0.70 in any epoch (every prior run had at least one epoch below 0.70). Context injection provides a knowledge floor and a stability floor, but this particular config (locked SDFT recipe + context) plateaus at ~0.725-0.73, same ceiling as the best non-context IS runs — context shifted the whole trajectory up and removed the early dip/late collapse, but did not raise the ceiling past ~0.73 within 10 epochs.
7. **Conclusion:** SUPPORTED (first baseline crossing, sustained stability) but not statistically significant (max 0.7433, need 0.7715). REVERT is not applicable — context injection is a genuine positive lever, kept as the base dataset for future experiments (both E046's LoRA attempt and the GRPO phase below build on it).
8. **New questions:** Does accuracy sustain above 0.74 through epoch 10? [ANSWERED: sustains 0.72-0.73, does not re-cross 0.74.] Is the context injection effect additive with other configs? Would combined dataset + context injection push higher? [Not run — campaign paused before these could be tried; see E046.]


---

## #E046 [CANCELLED]

1. **Motivation:** E045 (gpt-oss-20b full FT + context) crossed baseline at ep5 (0.7433) but zigzagged back to 0.72-0.73. LoRA on gpt-oss-20b failed (GQA dim mismatch in vLLM adapter loader). Pivoting to Qwen3-8B (student) + Qwen3-32B (teacher) — LoRA was validated on Qwen3-8B in v0.3.0. Smaller student + LoRA = faster iteration, implicit regularization, and higher LR (3e-4). Different model family tests whether the context injection + distillation approach generalizes beyond gpt-oss.
2. **Hypothesis (M9):** LoRA on Qwen3-8B with Qwen3-32B teacher + context injection achieves competitive accuracy. LoRA's rank-32 bottleneck acts as implicit regularization against k400 overfitting. Higher LR (3e-4) enables faster convergence.
3. **Prediction:** Need Qwen3-8B baseline first. Expect peak accuracy above baseline within 5 epochs. Less zigzag than E045 (consecutive epoch delta < 1.5pp). Falsified if: peak <= baseline after 10 epochs OR training destabilizes (loss spikes).
4. **Design:** MODEL_NAME=Qwen/Qwen3-8B, TEACHER_MODEL_PATH=Qwen/Qwen3-32B, TRAIN_MODE=lora, LORA_DIM=32, LORA_ALPHA=32, LR=3e-4, constant, IS_CAP=5.0, temp=1.2, online_feedback, ASYNC_ROLLOUT=1, context-injected k400 dataset. 10 epochs. GPU layout: 0 2 1 (1 vLLM, 2 trainers, 1 logprob).
5. **Results:** Never launched — a `sdft_qwen3_8b_run_46_lora` checkpoint dir exists (setup/smoke artifacts only, no eval results) — the run did not reach a scored epoch before being superseded.
6. **Analysis:** N/A — superseded before execution completed.
7. **Conclusion:** CANCELLED. The human stopped the self-distillation autoresearch campaign at this point and pivoted the whole project to GRPO (reward-driven RL against the existing reflector PASS/FAIL verdict) instead of continuing to search for self-distillation loss/model variants. This is a phase transition, not a refutation of M9 — the LoRA/Qwen3-8B idea is untested, not disproven.
8. **New questions:** N/A — see the GRPO phase (E047+) below for the new research direction.


---

# Phase 2: GRPO (reward-driven RL, self-distillation retired)

Self-distillation (reverse-KL against a frozen teacher) is retired as of E046.
25+ loss-geometry variants (IS aggregation, SFT anchor, JSD, per-token IS)
all plateaued at 0.69-0.73; the only lever that ever crossed baseline was
context injection (E045, input-signal not loss-geometry). Standing hypothesis
per `STATE.md`: reverse-KL optimizes distribution-matching to the teacher,
not correctness ("sharper but not smarter"). GRPO replaces the objective
entirely: on-policy group-relative policy gradient, reward = the reflector's
existing PASS/FAIL verdict (previously computed every rollout but only
logged, never trained on). No teacher, no distillation, no KL_COEF>0 in v1
(β=0 — biggest compute win, drops the 120b teacher from the critical path).
Implementation: `docs/megatron_trainer/grpo.md` (design spec) +
`plan_grpo.md` (this campaign's plan, repo root) on branch `ra/grpo-live`.
Runs on top of the context-injected dataset (E045's lever) since the context
is baked into the student's own rollout prompt (`prompt` field), not just
the teacher hint — confirmed by diffing the plain vs with-context files
directly.

---

## #E047 [RUNNING]

1. **Motivation:** First real GRPO training run (all prior GRPO work this
   session was implementation + infra smoke tests, not a science run).
   Implementation validated: 4 unit tests pass (gradient-identity vs plain
   PG, clip/IS engagement, degenerate-group math, truncation masking); a
   real perf bug (LM-head GEMM called per row-chunk instead of once) and a
   real crash bug (LoRA-mode logprob-server sync not gated on
   `USE_LOGPROB_SERVER`) were found and fixed via cluster smoke tests before
   this launch. `TRAIN_MODE` pivoted from `full` to `lora` mid-session: full
   FT OOM'd at the very first `optimizer.step()` (2-way FSDP shard of a 20B
   model leaves no room for AdamW state on an 80GB H100 — unrelated to
   GRPO, would hit SDFT too at this trainer count); LoRA sidesteps this
   entirely and issue #22 (the MoE adapter export bug that blocked LoRA on
   gpt-oss in E046) is now fixed (PR #23).
2. **Hypothesis:** Reward-driven on-policy RL (reflector PASS/FAIL verdict
   as reward) can push past the ~0.73 ceiling that 25+ reverse-KL
   loss-geometry variants couldn't reach, because it optimizes the eval
   criterion (correctness) directly instead of a distribution-matching
   proxy to a teacher.
3. **Prediction:** Peak accuracy > 0.7433 (beats best SDFT result, E045) by
   some checkpoint. Stretch: >= 0.7715 (statistically significant, +3pp
   McNemar threshold over baseline 0.7415). Health metrics
   (`grpo/entropy`, `grpo/clip_frac`) stay non-degenerate throughout —
   collapsing entropy or `clip_frac->0` with rising reward would indicate
   reward hacking / silent failure, not a real win. Falsified if: peak
   accuracy <= 0.7433 after a reasonable number of checkpoints, OR entropy
   collapses while pass rate does not correspondingly improve.
4. **Design:** `LOSS_TYPE=grpo`, `TRAIN_MODE=lora` (`LORA_DIM=32`,
   `LORA_ALPHA=32`), `GRPO_KL_COEF=0` (no reference/teacher forward),
   `GRPO_GROUPS=8`, `GRPO_ADV=mean`, `GRPO_CLIP_LOW/HIGH=0.2/0.28`,
   `GRPO_OLD_LOGPS=vllm`, `GRPO_IS_C_MAX=3.0`, `GRPO_LR=1e-5` (guess,
   unvalidated for GRPO+LoRA — literature SDFT-LoRA used 3e-4 on a
   different model/loss, not directly transferable; full-FT GRPO's
   literature default 1e-6 is calibrated for full-parameter updates, too
   conservative for LoRA's smaller effective space), `GRPO_LR_WARMUP_STEPS=15`,
   `GRPO_GRAD_CLIP=0.2`, `GRPO_MASK_TRUNCATED=1`, `GEN_TEMPERATURE=1.0`,
   `ASYNC_ROLLOUT=1` (group-atomic streaming producer), context-injected
   k400 dataset (`subset_k400_subset_with_context.jsonl`, 400 unique
   prompts). `GRAD_ACCUM_STEPS=40` (5 groups/step = 5 unique prompts/step
   across 5 trainer ranks, 1 group/rank). `NUM_EPOCHS=15` (one GRPO
   "epoch" = one pass over unique prompts = 80 steps here, so 15 epochs =
   1200 optimizer-step budget; will checkpoint-eval and likely stop well
   before exhausting this). `SAVE_EVERY=40` steps. GPU layout `0 5 1` (1
   vLLM + 5 trainers + 1 wasted logprob-server GPU = 7 of 8 total,
   **GPU 7 deliberately left free** for `auto_eval_poller.sh`, hardcoded
   `CUDA_VISIBLE_DEVICES=7` — matches E046's "GPU layout 0 2 1" convention
   of always leaving GPU 7 free). Run/output/wandb name
   `sdft_gptoss_20b_run_47_grpo_lora` (kept the `sdft_gptoss_20b_run_NN`
   convention so the existing `auto_eval_poller.sh` glob picks it up
   without modification; `poll_eval.sh run_47_grpo_lora step_40` etc. to
   check individual checkpoints).
   **Three launch attempts before this config stabilized:**
   (1) `0 4 3` (8 GPUs) broke the GPU-7-free convention, caught during
   boot, no progress lost. (2) `0 3 3` (3 vLLM instances, 7 GPUs) — tried
   because the smoke test's TIMING breakdown showed generation is ~90% of
   step wall-clock (`total=275.3s`, `producer_wait=249.0s`) vs training's
   26.2s (student+bwd+optim+wsync) — boot succeeded (all 3 vLLM ready,
   bootstrap adapter pushed to all 3), but ~11 min into Epoch 1, vLLM
   instance 0's generation throughput was stuck at ~1.2 tok/s (aggregate,
   12 "running" reqs) for 5+ minutes straight with zero recovery, causing
   ranks 1/2 to time out waiting on `_pull_microbatch`'s collective
   broadcast -> NCCL `DistBackendError` -> hard crash. Root cause not
   confirmed (160 CPU cores, load 82-170 at the time — not an obvious
   CPU-starvation smoking gun) but correlates with the untested 3-vLLM-
   instance config (only 1-instance had been smoke-tested). Multi-instance
   vLLM scaling deferred to a follow-up, not blocking this run.
   (3, **this launch**) `0 5 1` — reverts to the smoke-tested 1-vLLM-
   instance config (clean across 2 consecutive full steps in the smoke
   test, including adapter pushes) and scales trainer count instead
   (world_size 2->5) for more accumulation parallelism, since training
   compute is cheap under LoRA regardless of trainer count.
   **Also found+fixed while investigating checkpoint eval readiness**:
   `eval_with_retrieval.py` cannot load bare LoRA adapter checkpoints
   (`AutoConfig.from_pretrained` fails, no `model_type` in
   `adapter_config.json`) — this would have silently blocked every future
   LoRA run's eval (E046 never got far enough to hit it). Added
   `megatron_trainer/merge_lora_for_eval.py` (standard `peft.PeftModel.
   merge_and_unload()` on the HF-PEFT adapter format our trainer produces
   — NOT `megatron.bridge`'s own `examples/peft/merge_lora.py`, which
   expects a Megatron-native `dist_checkpointing` checkpoint and doesn't
   apply here) and wired it into `auto_eval_poller.sh` (merge-then-eval-
   then-delete-the-42GB-merge for any checkpoint with an
   `adapter_config.json`, base model read from the adapter config itself).
   Validated end-to-end on a smoke-test checkpoint before relying on it.
5. **Results:** _in progress._ Attempt 3 (`0 5 1`, `N_ASYNC=32` carried over
   unchanged from the `world_size=2` smoke test) also crashed, identical
   symptom (NCCL broadcast timeout at exactly 600s). Root cause this time:
   `_produce_streaming_grpo` caps in-flight groups at
   `n_groups_async = N_ASYNC // GRPO_GROUPS = 32/8 = 4`, but `world_size=5`
   needs 5 distinct groups concurrently (1/rank/step) — the 5th rank's group
   couldn't start generating until one of the first 4 finished, a second
   sequential generation round landing right on the timeout. Distinct bug
   from attempt 2's (that was 3 separate vLLM server processes with directly
   observed throughput collapse; this is single-instance producer pacing).
   **Attempt 4** (`0 5 1`, `N_ASYNC=40` = `world_size * GRPO_GROUPS` exactly)
   launched to fix this. Added a startup assertion in `trainer.py` so this
   config error fails fast at boot instead of ~10 min into training next
   time. Also: the auto-merge-eval poller fix picked up attempt 3's
   leftover `step_0` bootstrap checkpoint end-to-end (merge → eval →
   cleanup all succeeded) — accuracy **0.6553**, notably below the stated
   0.7415 baseline. Since `step_0` is the untrained bootstrap adapter
   (LoRA B-matrix inits to zero, so merged ≈ base model), this is a
   harness-comparability flag worth investigating (different eval dataset
   version, generation settings, or judge variance vs. whatever measured
   0.7415 originally) — noted, not yet root-caused, doesn't block E047.
   **Attempt 4 succeeded**: `opt_step=1` completed cleanly —
   `total=595.0s` (just under the 600s timeout that killed attempts 2/3),
   `producer_wait=567.4s`, checkpoint saved+pushed, process continued
   into step 2's generation without crashing. First-ever stable real
   (non-smoke) GRPO optimizer step for this campaign. Step 1's group was
   degenerate (`grpo/frac_reward_zero_std=1.0`, `loss=0.0`,
   `grad_norm=0.0`) — expected occasionally at G=8/pass-rate~0.65-0.74,
   not itself concerning; watching subsequent steps. Other metrics sane:
   `grpo/entropy=0.7831`, `grpo/clip_frac=0.0082`, `is/ratio_mean=0.9995`.
6. **Analysis:** 5 consecutive clean steps confirmed (1-5, ~386-595s each,
   all under the 600s NCCL timeout with comfortable margin on steps 3-5).
   **Attempt 4's `N_ASYNC=40` fix is confirmed working** — no recurrence
   of the timeout crash. Two notable early signals:
   (a) `grpo/frac_reward_zero_std`: 2 of 4 sampled steps fully degenerate
   (1.0) — a ~50% degenerate rate at n=4, much higher than a uniform-p
   estimate would predict (e.g. ~5.8% at p=0.7), consistent with the
   bimodal-difficulty hypothesis from open question #2. Small sample so
   far; watching wandb trend before deciding whether `GRPO_FILTER_GROUPS`
   is needed.
   (b) `opt_step=4` logged `grad_norm=335.41` vs `~0.017-0.018` on the
   other non-degenerate steps (2, 5) — a large raw-gradient spike, but
   `GRPO_GRAD_CLIP=0.2` via `clip_grad_norm_` means the *logged* grad_norm
   is the pre-clip diagnostic value; the actual applied update was clipped
   to norm 0.2 same as every other step. Not a real instability, just a
   sign the raw reward-gradient signal can be spiky — clipping is doing
   its job. No action needed unless clipped updates themselves start
   looking unstable (e.g. loss/entropy diverging).
7. **Conclusion (revised):** Attempt 4 crashed again at step 6 after 5
   clean steps — a *different*, unfixed failure mode. `vllm_0.log` shows
   generation throughput collapsing from ~190 tok/s to 16-28 tok/s for a
   sustained window while `Running: 40 reqs` held constant and GPU KV
   cache usage stayed ~5% (not memory/capacity-bound). Same symptom as
   attempt 2's collapse, now reproduced on a **single** vLLM instance —
   disproves the "multi-instance topology" theory from attempt 2's
   writeup; this is a genuine vLLM-under-load stability issue, apparently
   probabilistic rather than a hard concurrency threshold. Not root-caused
   (candidates: vLLM scheduler/batching defaults, `--enforce-eager`,
   MoE-routing-under-concurrency, CPU-bound request loop — none tested).
   Since no resume-from-checkpoint capability exists, **attempt 5** trades
   long-run ambition for guaranteed data: `SAVE_EVERY=5` (real checkpoint
   even from a short run) + a bounded (20x) auto-restart loop wrapping the
   launch so the campaign stays productive unattended, accepting that each
   restart trains a fresh LoRA init (no resumption, no `TRAINER_SEED`).
   Launched, awaiting first real checkpoint + poller eval.
9. **Hypothesis confirmed**: `auto_eval_poller.sh` had an infinite re-eval
   bug (blindly echoed "DONE" regardless of `eval_with_retrieval.py`'s exit
   code, never marked failures) — `run_44_long/epoch_46` (unrelated,
   fails to load) was being re-evaluated on **every 5-min scan cycle**
   back-to-back, continuously loading/hammering GPU 7 for the entire
   window E047's retry loop was crashing within 1-2 steps each try. Killed
   the poller at ~00:40 UTC; **the exact same training config immediately
   went from crashing every 1-2 steps to 6 consecutive clean steps**
   (steps 1-6, all 286-524s, comfortable margin under the 600s timeout) —
   strong causal evidence podman's device-level-only GPU isolation let the
   poller's continuous eval load (GPU 7, shared host CPU/PCIe/NVMe) starve
   the training vLLM instance (GPU 0). Fixed the poller (exit-code check +
   `.eval_failed` marker, no more infinite retries) and will restart it
   once more clean steps confirm this wasn't partly coincidental.
   Degenerate-group rate across both runs now ~60% (6/10 steps) — high
   enough to prioritize `GRPO_FILTER_GROUPS` as a near-term follow-up.
10. **Follow-up (deferred, tracked separately):** (a) even with the poller
   bug fixed, watch whether its *normal* (non-buggy) periodic eval activity
   still causes occasional interference — may need to pin the poller to
   not run *during* active training steps, or accept brief slowdowns;
   (b) build proper checkpoint-resume (adapter + optimizer + scheduler +
   step counter) so restarts don't lose progress; (c) `GRPO_FILTER_GROUPS`
   given the consistently high (~60%) degenerate-group rate.
8. **New questions:** Why does step_0 (merged, untrained) score 0.6553 vs
   the 0.7415 baseline under the same `poll_eval.sh`/`eval_with_retrieval.py`
   harness — different eval set version, generation config, or merge
   artifact? Needs a direct diff of eval configs before trusting absolute
   accuracy numbers from this harness path (relative deltas across E047's
   own checkpoints are less affected either way).

11. **Accuracy trend so far (steps 0-10, noisy, no positive signal yet):**
    step_0=0.6553, step_1=0.6535, step_2=0.6535, step_3=0.6463,
    step_4=0.6284, step_5=0.6463, step_10=0.6463 (native-LoRA re-run of
    step_10: 0.6715, ~2.5pp higher — see below). Flat-to-slightly-declining
    over the first 10 steps; not yet distinguishable from noise given the
    eval's own run-to-run variance (see next point) and the tiny amount of
    training so far (10 steps, LR=1e-5, ~60% degenerate-group rate meaning
    many of those steps contributed near-zero gradient). Too early to call
    — need many more steps before drawing conclusions either way.
12. **Native LoRA eval support added** (avoids merging every checkpoint):
    `eval_with_retrieval.py` (separate repo, `eshwarprasadS/maas-
    knowledge-eval`) now detects `adapter_config.json`, loads the base
    model via vLLM with `enable_lora=True` + `max_lora_rank` (from the
    adapter's `r` field), and hot-attaches the adapter via `LoRARequest`
    at generate time — no full-checkpoint merge, no extra ~42GB copy per
    eval. vLLM 0.25.1 has native "fused MoE LoRA" support, handling our
    `target_parameters`-based fused-expert adapter format directly.
    Validated on step_10: native gives **0.6715** vs the merge-based
    **0.6463** for the identical checkpoint — a 2.5pp gap, plausibly
    sampling/judge noise (`temperature=0.7`, no fixed seed in this eval
    script) rather than a correctness bug, but flagging: **absolute
    accuracy numbers from this harness have at least ~2.5pp run-to-run
    noise on top of the previously-known ~1.5pp McNemar paired SE** — the
    true noise floor for *unpaired* single-run comparisons (i.e. comparing
    two different checkpoints, each eval'd once) is probably closer to
    3-4pp combined. Don't over-read single-run deltas smaller than that.
    `auto_eval_poller.sh` simplified accordingly (merge/podman step
    removed — also one less heavy subprocess per eval cycle, may help the
    residual contention risk below). File changes deployed to node05 but
    not committed to that repo (not mine to push to without asking).
13. **Refined poller-contention finding**: after fixing the infinite-retry
    bug and confirming 11 clean steps with the poller off, restarted the
    fixed poller — it ran one real eval and training crashed ~6 min into
    that eval's window (attempt 3→4 in the retry loop, recovered
    automatically within 10s). So the infinite-retry bug was the
    *dominant* contributor to instability, but a single *legitimate* eval
    can still occasionally cause enough host-level contention to trip the
    600s timeout. Accepting this residual risk for now (retry loop
    recovers automatically); proper fix (process/CPU isolation, or
    scheduling eval around active training) is a deferred follow-up.
14. **`SAVE_EVERY` fixed for LoRA mode**: was silently saving a full
    checkpoint dir at *every* optimizer step regardless of `SAVE_EVERY`
    (a side effect of `push_lora_adapter`'s hot-swap needing an on-disk
    adapter to load into vLLM). Fixed in `trainer.py`: delete the
    transient per-step dir after a successful push when the step isn't a
    `SAVE_EVERY` multiple. Cleaned up E047's existing non-multiple dirs.
15. **`gpg_rescale` bug found via PR #24 comparison, fixed**: computed
    per-rank-local (`num_groups_local`/`num_nondeg_local`), which at our
    actual config (`groups_per_step=1`) can only ever be `1/1=1.0`
    (no-op) or `0/1->10000` (multiplying an already-zero degenerate
    loss — still a no-op). The cross-rank compensation for degenerate
    groups had been completely inert the whole campaign. Fixed:
    all-reduce `[num_groups_local, num_nondeg_local]` across ranks before
    computing the rescale (`trainer.py`, `7da66ac`). Post-fix,
    `grpo/gpg_rescale` now shows varied real values (2.5, 5.0, 50000.0)
    instead of being stuck at 1.0/10000.0 — confirmed working. All 4
    unit tests re-verified passing. This may explain the flat/no-progress
    trend through step 10 (~60% degenerate rate meant most steps'
    survivor gradients were never actually boosted to compensate).
16. **Checkpoint-deletion race condition — major crash-loop root cause,
    found and fixed in two iterations.** After the gpg fix, ~7 consecutive
    retry-loop attempts crashed with vLLM `"EngineCore encountered an
    issue"` (500 errors on `/v1/completions`) — always immediately after
    `opt_step=1`, the first step where `trainer.py`'s SAVE_EVERY cleanup
    (`shutil.rmtree` on the just-pushed LoRA adapter dir) fires. Root
    cause: the code assumed vLLM's `/v1/load_lora_adapter` HTTP response
    returning success means the adapter file is fully done being read,
    but safetensors/mmap-based loads can page in lazily, and
    `--max-cpu-loras=2` means vLLM's LRU adapter cache can hold more than
    the immediately-previous generation — deleting the file out from
    under a still-referenced mmap crashes the engine.
    - **Iteration 1** (`0c17fe1`): delay deletion by 1 step (only delete
      the previous transient dir once the *next* push succeeds).
      Insufficient — two separate attempts each reached `opt_step=2` for
      the first time ever, then crashed ~8s later, exactly when the
      first real `rmtree` fired under the new logic (just shifted the
      bug by one step).
    - **Iteration 2** (`6beda94`): widened to a 3-slot FIFO delay
      (comfortably above `max_cpu_loras=2`) — only delete once a dir
      falls 3 pushes out of date. **Confirmed working**: one attempt
      cleanly passed steps 1-5 (including step 4, the first real delete
      point under this logic) with zero delete-related crashes before
      eventually hitting an unrelated NCCL timeout (see #17). The
      original crash-the-instant-you-delete-anything pattern is gone.
17. **Separate, pre-existing issue surfaced once #16 was fixed: vLLM
    generation throughput stalls severe enough to blow the 600s NCCL
    collective timeout.** This is the same unfixed "vLLM throughput
    collapse under sustained load" issue from point 7/#9 above — not new,
    but now the dominant blocker since the delete-race no longer masks
    it. Confirmed via `Watchdog caught collective operation timeout:
    WorkNCCL(..., Timeout(ms)=600000) ran for 600042 milliseconds` on all
    4 non-rank-0 trainers simultaneously, at varying points across 4
    separate attempts (before step 1, after step 2, after step 3, after
    step 5) — no fixed threshold, consistent with the probabilistic
    framing already on record. **Mitigation deployed** (not a root-cause
    fix): `model_utils.py`'s `init_distributed_trainer()` now passes an
    explicit `timeout=timedelta(seconds=1800)` to `dist.init_process_group`
    (was implicitly defaulting to 600s), overridable via `NCCL_TIMEOUT_SEC`.
    Gives stalled-but-recovering generation 3x the runway before the whole
    run is hard-killed. Root cause (vLLM scheduler defaults,
    `--enforce-eager`, MoE-routing-under-concurrency, or CPU-bound request
    loop) still not isolated — deferred.
18. **Also found (not yet ported/fixed): missing tuned MoE kernel config**
    for `E=32,N=2880,device_name=NVIDIA_H100_80GB_HBM3` — both the
    training vLLM (0.20.2) and eval vLLM (0.25.1) instances log
    `"Using default MoE config. Performance might be sub-optimal!"` at
    startup, plus live Triton JIT compilation during inference (not
    warmup) for the fused-MoE and fused-MoE-LoRA kernels. vLLM ships an
    official tuning script (`benchmarks/kernels/benchmark_moe.py`) for
    exactly this gap; not yet run. Plausible contributor to both the
    generally-low aggregate decode throughput (~130-190 tok/s healthy,
    dipping to ~24-32 tok/s) and the NCCL-timeout stalls in #17, but
    unconfirmed magnitude — flagged as a follow-up, not yet actioned.
19. **Reflector cost confirmed avoidable, now implemented**: direct timing
    test (isolated, same model/creds) shows the reflector call costs
    8.33s avg (up to 12.24s) at the current `max_tokens=2048`
    detailed-feedback setting, vs 0.89s avg with a verdict-only
    `max_tokens=64` variant — a ~9.4x speedup on that call. GRPO only
    consumes `verdict` (`trainer.py:138-140` already notes the feedback
    text is unused for GRPO). Implemented in `reflector.py`
    (`verdict_only` param, `c5361a8`), wired through `RagEnv` and
    `trainer.py`'s `_build_env` (`verdict_only=(LOSS_TYPE=="grpo")`).
    Staged (committed+pulled to node05) but deliberately not deployed
    into the currently-stable run yet — see #20.
20. **MAJOR MILESTONE: crash-loop resolved, first stable multi-step GRPO
    trajectory.** With all three fixes combined (gpg_rescale global
    all-reduce, 3-step checkpoint-delete delay, 1800s NCCL timeout),
    launched a fresh attempt at 17:16:47 UTC — **still running with zero
    crashes 95+ minutes later, reached opt_step=11** (vs the previous
    best of 5 steps before a crash). Loss/grad_norm values are now
    visibly non-degenerate and varied (0.95, 1.16, 0.20 — not the earlier
    flat-zero pattern), `grpo/gpg_rescale` shows real varying values
    (2.5, 5.0), consistent with the gpg fix finally letting gradient
    signal flow correctly. This is the first genuinely healthy,
    uninterrupted GRPO training trajectory of the whole campaign.
    Per-step accuracy so far (steps 0-9, via the poller catching each
    transient checkpoint before its 3-step-delayed cleanup — an
    unplanned bonus of dense eval coverage): 0.6463, 0.632, 0.6373,
    0.6517, 0.6463, 0.6589, 0.6373, 0.6661, 0.6212, 0.6427 — still flat/
    noisy in the 0.62-0.67 band, no clear trend yet. Expected at this
    stage: only 11 steps at a conservative `GRPO_LR=1e-5`, and this is
    the FIRST time gradients have actually been non-degenerate for a
    sustained run — prior steps-0-10 data (recorded in #11 above) came
    from underpowered (gpg-bugged) gradients on a since-superseded
    LoRA init.     Continuing to monitor for real signal to emerge with more
    steps; NCCL_TIMEOUT_SEC=1800 mitigation and reflector verdict-only
    fast-path remain available levers if/when needed.
21. **`GRPO_FILTER_GROUPS` implemented and deployed** (`0a22bc5`,
    `096c2c8`), replacing the `NotImplementedError` stub. Motivated by
    quantifying #20's flat accuracy: pulled `frac_reward_zero_std` for
    all 27 real steps of the stable run — averaged ~80% degenerate
    (7/27 steps had ALL 5 groups degenerate = zero gradient that whole
    step), far above the ~5.8% uniform-pass-rate estimate, consistent
    with a bimodal per-prompt difficulty distribution (some prompts the
    student always/never solves at G=8). This means ~80% of generation
    compute across the whole 27-step run contributed no training signal
    at all — a much stronger candidate explanation for the flat
    trajectory than "just needs more steps." Implementation: a degenerate
    group (`_push_group`'s exact formula: `len(set(rewards)) == 1`)
    triggers resampling a fresh prompt, up to `GRPO_MAX_GEN_BATCHES=10`
    attempts, inside `_produce_streaming_grpo`'s `run_group` (thread-safe
    `data_iter` access via a lock, since resampling now calls `next()`
    from worker threads too, not just the producer loop). Verified via a
    standalone mock-based logic test (cap enforcement, disabled-state
    passthrough, cap=1 edge case) before deploying, given the risk of a
    subtle threading/deadlock bug in the live run. Deployed by
    interrupting the otherwise-healthy 28-step stable run from #20 —
    judged worthwhile since that run's gradients, while finally
    non-degenerate at the infrastructure level, were still ~80%
    wasted at the data level; continuing it further without this fix
    would not have been genuine progress toward beating baseline.
    Expect: higher per-step generation cost (more resample rounds), but
    much higher signal density per step. Watching `frac_reward_zero_std`
    (should drop sharply, ideally near 0 outside rare cap-exhaustion
    cases) and step timing (expected to increase) as the primary
    validation signals, then eval accuracy trend over more steps.
